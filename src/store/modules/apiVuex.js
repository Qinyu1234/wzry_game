const state = {
    heroInfo:{}
}
const mutations = {
    SETHEROINFO(state,heroInfo){
        state.heroInfo = heroInfo
    }
}
const actions = {}
const getters = {}
export default {
    state,
    mutations,
    actions,
    getters
}